import {ParseUnit} from "./parse-unit.class";
import {AssertionResult} from "../interfaces/assertions.interface";

export class GraphLink extends ParseUnit {

    trigger: string;
    targetNode: string;

}