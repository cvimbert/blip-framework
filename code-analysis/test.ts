import {BlpParser} from "../blp-parser/blp-parser.class";
import {CodeStructure} from "../blp-parser/interfaces/assertions.interface";

let parser: BlpParser = new BlpParser("code.blp");


/*let structure: CodeStructure = {
    structures: {
        root: {
            assertions: [
                {
                    expression: /a/,
                    values: []
                },
            ]
        }
    },
    root: "root"
};*/
